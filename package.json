{
  "name": "spotify-stats",
  "version": "1.0.1",
  "description": "Fetch and display Spotify top artists",
  "main": "spotify.js",
  "scripts": {
    "start": "node spotify.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "spotify-web-api-node": "^5.0.2",
    "@notionhq/client": "^2.2.10",
    "dotenv": "^16.0.3"
  },
  "engines": {
    "node": ">=20.0.0" 
  }
}
